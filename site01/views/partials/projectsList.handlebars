{{#if data.projects}}
    <div class="container-out container-light">
        <div class="row">
            <div class="col-md-9">
                <div id="content">
                    <ul class="arrow">
                        {{#each data.projects}}
                            <li><a href="#{{this.slug}}">{{this.name}}</a></li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
{{/if}}
