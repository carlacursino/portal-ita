{{#if data.projects}}
    <div id="content">
        <ul class="arrow">
            {{#each data.projects}}
                <li><a href="/{{../language}}/project/{{this._id}}">{{this.name}}</a></li>
            {{/each}}
        </ul>
    </div>
{{/if}}
